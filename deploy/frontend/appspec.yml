version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/frontend-code-deploy
hooks:
  BeforeInstall:
    - location: stop.sh
      timeout: 30
  AfterInstall:
    - location: install.sh
      timeout: 60
  ApplicationStart:
    - location: start.sh
      timeout: 30
